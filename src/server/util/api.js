// channel
import {updateChannel} from '../channel/update';
// team
import {inviteToTeam} from '../team/invite';

export const api = {
    channel: {
        updateChannel,
    },
    team: {
        inviteToTeam,
    },
};
